<?xml version="1.0" encoding="UTF-8"?>

<atlassian-plugin key="${project.groupId}.${project.artifactId}" name="${project.name}" plugins-version="2">
    <plugin-info>
      <description>${project.description}</description>
      <version>${project.version}</version>
      <vendor name="${project.organization.name}" url="${project.organization.url}"/>
    </plugin-info>

    <buildAgentRequirementFilter key="bambooAgentAccessControlFilter" name="Remote Build Agent Access Control Filter"
                                 class="mondraymond.plugins.bamboo.RemoteAgentAccessControlFilter">
        <description>Enables ACLs for Bamboo remote agents</description>
    </buildAgentRequirementFilter>

</atlassian-plugin>